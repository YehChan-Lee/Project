<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Reservation">

	<!-- footer 예약수 -->
	<select id="footerreserve" resultType="int">
		select count(*) from
		Reservation
	</select>

	<select id="getAll_shopId" parameterType="String"
		resultType="Reservation">
	<![CDATA[select * from RESERVATION where SHOP_ID=#{shop_id} ]]>
	</select>

	<insert id="insertReservation" parameterType="Reservation">
      <![CDATA[
      INSERT INTO "BP"."RESERVATION" (USER_EMAIL, SHOP_TITLE, RES_DATE, RES_CUSTOMER, SHOP_ID,RESERVE_IDX,REV_PHONE,RES_NAME ) VALUES (#{user_email},#{shop_title},#{res_date},#{res_customer},#{shop_id},SEQ_RESERVE_IDX.NEXTVAL,#{rev_phone},#{res_name})
      ]]>
	</insert>

	<update id="updateAlertNew_shopId" parameterType="String">
   <![CDATA[
   UPDATE RESERVATION SET ALERT_NEW = '0' WHERE SHOP_ID = #{shop_id}
   ]]>
	</update>

	<select id="reservationOne" parameterType="String"
		resultType="Reservation">
		<![CDATA[
		SELECT * FROM RESERVATION WHERE SHOP_ID=#{Shop_id}
		]]>
	</select>

</mapper>