<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Notice">	
	<select id="noticeList" resultType="Notice">
		<![CDATA[SELECT * FROM Notice]]> 		
	</select>
	
	<!--  -->
	<select id="pageList" resultType="Notice" parameterType="com.javaex.model.Criteria">
		select notice_idx, notice_title, notice_content, notice_date
		from (
		    select notice_idx, notice_title, notice_content, notice_date,
		            ROW_NUMBER() OVER (ORDER BY notice_idx DESC) as rNum
		            from Notice) N
		where rNum between #{rowStart} and #{rowEnd}
		order by notice_idx DESC
	</select>
	
	<select id="pageCount" resultType="int">
		select count(notice_idx)
		from Notice
		where notice_idx>0
	</select>
	<!--  -->
	<select id="getNoticeCnt" resultType="int">
		select count(*) from Notice
	</select>
		
	<insert id="noticeInsert">
		<![CDATA[
	
		]]>
	</insert>
	
	<update id="noticeUpdate">
		<![CDATA[

		]]>	
	</update>
	
	<delete id="noticeDelete">
		<![CDATA[

		]]>	
	</delete>
</mapper>